<?xml version="1.0" encoding="UTF-8"?>
<component type="desktop-application">
  <id>io.github.marcelogomes90.cosmic-ext-applet-status-hub</id>
  <metadata_license>CC0-1.0</metadata_license>
  <project_license>GPL-3.0-only</project_license>
  <project_group>COSMIC</project_group>
  <developer id="io.github.marcelogomes90">
    <name>marcelogomes90</name>
  </developer>
  <name>Status Hub</name>
  <summary>Application tray items, gathered behind a single panel button</summary>
  <description>
    <p>Status Hub is a StatusNotifierItem tray for the COSMIC desktop. It adds a hub button to the panel; clicking it opens a compact popup holding the tray items and wraps them onto additional rows when there are many. Items reached for often can be pinned beside the hub so they stay one click away. A popup item's menu stays in the same card behind a divider, while a pinned item's menu is anchored to its panel slot.</p>
    <p>It is an independent implementation written from the StatusNotifierItem and DBusMenu specifications. It is not a fork of COSMIC's Status Area, and it is built to run alongside it.</p>
    <p>The tray is the part of a desktop most exposed to badly behaved applications: they hang, they exit without warning, they answer with the wrong types, and they restart under a new bus name a millisecond after the old one vanished. Status Hub is organised around not letting any of that become your problem:</p>
    <ul>
      <li>An item exists exactly as long as its bus owner exists, so no leftover icons are stranded when an application exits without unregistering.</li>
      <li>Every remote call has a timeout and every item has its own error budget, so one wedged application cannot delay another, or the popup, or the panel.</li>
      <li>Every asynchronous reply carries a generation token, so a reply belonging to a process that has already exited can never update its successor.</li>
      <li>Ordering is a pure function of discovery order and a small remembered list, never of the order in which replies happened to arrive, so every panel shows the same order.</li>
      <li>The applet dies with the panel and is relaunched by it, rebuilding its entire state on every start, so restarting the panel never requires restarting a tray application.</li>
    </ul>
  </description>
  <screenshots>
    <screenshot type="default">
      <image>https://raw.githubusercontent.com/marcelogomes90/cosmic-ext-applet-status-hub/main/resources/screenshots/tray-popup.png</image>
      <caption>Tray items gathered in the popup, under a header that is always there</caption>
    </screenshot>
    <screenshot>
      <image>https://raw.githubusercontent.com/marcelogomes90/cosmic-ext-applet-status-hub/main/resources/screenshots/item-menu.png</image>
      <caption>An application's tray menu, opened from the popup</caption>
    </screenshot>
    <screenshot>
      <image>https://raw.githubusercontent.com/marcelogomes90/cosmic-ext-applet-status-hub/main/resources/screenshots/settings.png</image>
      <caption>The settings view, where any item can be pinned to the panel</caption>
    </screenshot>
    <screenshot>
      <image>https://raw.githubusercontent.com/marcelogomes90/cosmic-ext-applet-status-hub/main/resources/screenshots/pinned-item-menu.png</image>
      <caption>A pinned item's menu, opened straight from the panel</caption>
    </screenshot>
  </screenshots>
  <launchable type="desktop-id">io.github.marcelogomes90.cosmic-ext-applet-status-hub.desktop</launchable>
  <url type="homepage">https://github.com/marcelogomes90/cosmic-ext-applet-status-hub</url>
  <url type="bugtracker">https://github.com/marcelogomes90/cosmic-ext-applet-status-hub/issues</url>
  <url type="vcs-browser">https://github.com/marcelogomes90/cosmic-ext-applet-status-hub</url>
  <provides>
    <id>com.system76.CosmicApplet</id>
    <binary>cosmic-status-hub</binary>
  </provides>
  <requires>
    <display_length compare="ge">360</display_length>
  </requires>
  <supports>
    <control>keyboard</control>
    <control>pointing</control>
  </supports>
  <categories>
    <category>Utility</category>
  </categories>
  <keywords>
    <keyword>COSMIC</keyword>
    <keyword>Applet</keyword>
    <keyword>Tray</keyword>
    <keyword>Indicator</keyword>
    <keyword>Status</keyword>
    <keyword>SNI</keyword>
    <keyword>StatusNotifierItem</keyword>
    <keyword>DBusMenu</keyword>
    <keyword>Panel</keyword>
  </keywords>
  <releases>
    <release type="stable" version="1.2.0" date="2026-08-30">
      <description>
        <p>Icons an application publishes from inside its own sandbox now resolve, tray artwork is adapted to the panel it sits on, and the Flatpak asks for far fewer permissions.</p>
        <ul>
          <li>Finds an icon whose directory means something only inside the publishing application's own sandbox, by looking for the same artwork in that application's Flatpak payload. Dropbox and Steam show the icons they ship instead of a generic one, and the rule carries no per-application knowledge, so any application publishing such a path is covered.</li>
          <li>Adapts achromatic tray artwork to the panel, so a glyph drawn for a dark panel no longer disappears on a light one. Artwork that already reads is left untouched, judged by how much of its ink contrasts with the panel and whether that ink traces the whole shape, rather than by an average tone that no part of the artwork actually has.</li>
          <li>Asks for eleven permissions instead of fifteen. Read access to every Flatpak application's private data, to the host system tree and to the user's icon directory is gone, since Flatpak already exposes the host icon themes on its own; both export grants are narrowed to their icons subtree.</li>
        </ul>
      </description>
    </release>
    <release type="stable" version="1.1.0" date="2026-08-29">
      <description>
        <p>Items can be pinned to the panel, the popup gained a settings view, and the interface is available in ten languages.</p>
        <ul>
          <li>Pin any tray item to the panel, where it sits beside the hub button and stays one click away. Pins are remembered between sessions, survive an application restarting under a new bus name, and are kept in step across every panel.</li>
          <li>Adds a header to the popup, with a settings view behind it listing every item and a switch for pinning it. Choices are applied when they are saved and discarded if the popup is dismissed, so the panel never shifts while they are being made.</li>
          <li>Replaces the panel chevron with a button that no longer changes with the popup state, and keeps it at the end of the applet that faces away from the screen, so pinning an item does not move it.</li>
          <li>Keeps a popup item's menu in the same card behind a divider, while a menu opened from a pinned item is anchored to that item's own panel slot.</li>
          <li>Prefers the icon theme over the icon file an application ships alongside itself, so the tray follows the theme where one is available.</li>
          <li>Shows the panel button even when the tray is empty, so the settings view can always be reached, and centres an explanatory notice in the popup.</li>
          <li>Translated interface in English, Brazilian Portuguese, Dutch, French, German, Italian, Russian, Simplified Chinese, Spanish, and Ukrainian.</li>
        </ul>
      </description>
    </release>
    <release type="stable" version="1.0.2" date="2026-08-27">
      <description>
        <p>Left click brings the application to the front again, and the tray follows the usual mouse controls.</p>
        <ul>
          <li>Asks the panel for its privileged Wayland connection, which is what lets the compositor grant a real activation token. Without it the token was issued but only marked the window urgent, so an application such as ZapZap was told to activate and never came to the front.</li>
          <li>Restores the standard tray controls: left click activates the application, middle click requests its optional secondary action, and right click opens its menu. Left click no longer falls back to the menu when activation fails, and no longer defers to the menu for items that advertise one.</li>
          <li>Finds an icon the application publishes under its own IconThemePath before consulting the icon theme, and only accepts a result that really lives there, so an icon theme carrying a same-named copy can no longer win.</li>
          <li>Retries an icon that fell back to the generic one, on a schedule stretching to about a minute, for applications that publish their icon shortly after registering.</li>
          <li>Coalesces menu layout requests and discards a layout older than the one already shown, so an application redrawing its menu repeatedly cannot flicker or go backwards.</li>
          <li>Stops a slow menu announcement from delaying the first layout, so a menu opens as soon as its contents are known.</li>
          <li>Widens the popup only when a menu is on screen, leaving the tray at the width its icons need.</li>
          <li>Collapses the panel button to nothing while there are no tray items, instead of holding an empty gap.</li>
        </ul>
      </description>
    </release>
    <release type="stable" version="1.0.1" date="2026-08-26">
      <description>
        <p>Icons published by Flatpak applications are found again.</p>
        <ul>
          <li>Reads the Flatpak installation directories, so the icons exported by a Flatpak application resolve. They are symbolic links into those directories, and without them every such icon fell back to the generic one whenever the active icon theme did not happen to carry a copy.</li>
          <li>Reads the per-application Flatpak data directories, where an application such as Steam keeps the icon it names in IconThemePath. Read only, and only what those applications publish.</li>
          <li>Looks an icon name up once, letting the path the item published win over the icon theme.</li>
          <li>Ignores an empty icon theme path instead of searching it as a relative directory.</li>
          <li>Reports the file each icon resolved to, so a missing icon can be diagnosed from the log.</li>
          <li>Reads the COSMIC configuration without asking to write it.</li>
          <li>Asks again when an application answers only part of its properties. A slow application could leave an item with no menu, and therefore no right click, until it was restarted.</li>
          <li>Keeps retrying for about two minutes rather than twelve seconds, which is closer to how long a busy application takes to answer.</li>
        </ul>
      </description>
    </release>
    <release type="stable" version="1.0.0" date="2026-08-26">
      <description>
        <p>First release.</p>
        <ul>
          <li>Collects StatusNotifierItem tray items behind a single panel button.</li>
          <li>Renders DBusMenu menus, including submenus, checkmarks, and radio groups.</li>
          <li>Removes items when their bus owner disappears, so no icon is left stranded.</li>
          <li>Applies a timeout and a per-item error budget to every remote call.</li>
          <li>Keeps a stable item order that does not depend on reply timing.</li>
          <li>Runs alongside COSMIC's own Status Area.</li>
        </ul>
      </description>
    </release>
  </releases>
  <content_rating type="oars-1.1" />
</component>
